services:
  - type: web
    name: jobseek-api
    env: python
    buildCommand: pip install -r requirements-fallback.txt
    startCommand: python fallback_app.py
    envVars:
      - key: PORT
        value: 10000